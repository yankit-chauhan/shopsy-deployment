apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: shopsy
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - port: 80
      targetPort: 8082
      protocol: TCP
